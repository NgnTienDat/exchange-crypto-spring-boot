server:
  port: 8080
spring:
  application:
    name: exchange-crypto

  datasource:
    #    url: ${DBMS_URL:jdbc:mysql://localhost:3306/socialnetworkdb}
    #    username: ${DBMS_USERNAME:root}
    #    password: ${DBMS_PASSWORD:tiendatmySQL964@}

    url: ${DBMS_CONNECTION:jdbc:mysql://localhost:3306/exchangecrypto}
    username: ${DBMS_USERNAME:root}
    password: ${DBMS_PASSWORD:tiendatmySQL964@}
    driver-class-name: "com.mysql.cj.jdbc.Driver"

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

auth:
  signer-key: "UwQQGr3/Fi21btHXjPqoUSPFF6X9TSBLtPBoerP2+ykB7HfNGs4g940yZWelH+MV"
  valid-duration: 86400 # in second : 1 day
  refreshable-duration: 259200 # in second
